const slowFadeDuration = Duration(milliseconds: 600);
const autoSlideDownDuration = Duration(milliseconds: 850);
const standardFadeDuration = Duration(milliseconds: 300);
const fastFadeDuration = Duration(milliseconds: 150);
const scoreTileHeight = 56.0;
const scoreIndicatorHeight = 120.0;

const diceDotSize = 5.0;
const diceFaceSize = 72.0;
